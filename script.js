const carName = document.getElementById('car-name'); const carDescription = document.getElementById('car-description'); const carImage = document.getElementById('car-image'); const carMake = document.getElementById('car-make'); const carModel = document.getElementById('car-model'); const carYear = document.getElementById('car-year'); const carPrice = document.getElementById('car-price'); function loadCarDetails(car) { carName.textContent = car.name; carDescription.textContent = car.description; carImage.src = car.image; carMake.textContent = car.make; carModel.textContent = car.model; carYear.textContent = car.year; carPrice.textContent = car.price; }